import { defineComponent } from 'vue';
import { LoadingIcon, CloseIcon, MenuFoldIcon, MenuUnfoldIcon } from 'tdesign-icons-vue-next';
export default defineComponent({
    components: {
        LoadingIcon,
        CloseIcon,
        MenuFoldIcon,
        MenuUnfoldIcon,
    },
});